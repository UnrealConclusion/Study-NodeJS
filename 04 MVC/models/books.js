const fs = require('fs');
const path = require('path');



class Book {
    #title;
    constructor(title) {
        this.title = title;
    }

    // save itself in the database
    save() {

    }

    static fetchAll() {

    }
}